# 개요
데이터를 크롤링 해서 실시간 검색어 랭킹을 보여주는 서비스 구현합니다.

# 사용 예정 기술 스택
- 스프링 부트
- 엘라스틱 서치
- 카프카
- 레디스

# 프로젝트 구조
멀티 모듈 프로젝트로 수집, 정제, 색인, 서빙 총 4개의 모듈로 구성됩니다.

## 수집 시스템 모듈
데이터를 수집하여 카프카 메시지로 정제 시스템으로 발송합니다.

### 대용량 파일 크롤링 방식
- https://dumps.wikimedia.org/kowiki/ 해당 사이트에서 제공하는 위키데이터 파일을 사용합니다.
- 위의 대용량 파일을 읽어서 카프카 메시지로 발송합니다.
- 대용량 파일을 핸들링 해야 합니다.
- 파일을 1MB 이하의 메시지로 분할해서 전송해야 합니다.

### HTML 크롤링 방식
- 정기적으로 하루치 네이버 뉴스 페이지의 HTML을 크롤링해 카프카 메시지로 발송합니다.
- 페이지 내 링크를 자동으로 찾아가는 방식으로 크롤링 해야합니다.
- RSS를 활용하는 방안도 고민 중입니다.

### API 연동 방식
- 트위터(X)의 Developer API를 활용해 크롤링 해 카프카 메시지로 발송합니다.

## 정제 시스템 모듈
수집 시스템으로부터 카프카 메시지를 수신하고 데이터를 정제해 색인 시스템으로 카프카 메시지를 발송합니다.

## 색인 시스템 모듈
정제 시스템으로부터 카프카 메시지를 수신하고 엘라스틱 서치로 데이터를 색인합니다.

## 서빙 시스템 모듈
엘라스틱 서치 쿼리 및 레디스 캐싱을 수행합니다.